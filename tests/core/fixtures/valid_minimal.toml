# Minimal valid manifest fixture.
# Only the fields the load path requires.

[bookwright]
cli_version_min = "0.0.1"
schema_version = "golem-1.1"
manifest_version = "1"
uri_base = "https://example.org/minimal/"

[book]
title = "Minimal Book"
type = "essay"
language = "en"
authors = ["Solo Author"]

[integration]
key = "generic"
skills_dir = ".agents/skills"
