---
name: "The Levee Breaks"
sequence: "Act I"
order: 2
---

A second `Act I` unit card sharing `opening.md`'s `sequence` with a later `order`,
so the live parity build assembles a `NarrativeSequence` (G7) with two ordered
`dlp:proper-part` members — G7 is observed alive against a real build, never
hand-listed (iteration 029, FR-014).
