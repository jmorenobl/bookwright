---
name: "The Flood Arrives"
functions: [interdiction, departure]
roles: [protagonist]
sequence: "Act I"
order: 1
---

One narrative-unit card so the live parity build emits G9 (NarrativeUnit) and G10
(NarrativeFunction) `rdf:type` IRIs and exercises role resolution against Ada
Finch's character-scoped `protagonist` role node (iteration 028, FR-013). Its
`sequence`/`order` keys, shared with `rising.md`, make the build assemble a
`NarrativeSequence` (G7) with two ordered members so G7 is observed alive
(iteration 029, FR-014).
