---
name: "The Lighthouse"
---

A second `NarrativeLocation` with no `setting:` — a bare G13 node, no
`dlp:generic-location` edge. It keeps the parity build honest about the
no-setting path (FR-002).
