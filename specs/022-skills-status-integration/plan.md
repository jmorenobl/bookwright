# Implementation Plan: 022-skills-status-integration

**Branch**: `022-skills-status-integration` | **Date**: 2026-06-12 | **Spec**: [spec.md](file:///Users/jorge/Projects/bookwright/specs/022-skills-status-integration/spec.md)

**Input**: Feature specification from `/specs/022-skills-status-integration/spec.md`

## Summary

This iteration updates the `SKILL.md` materializer to inject `bookwright status --json` logic at the start and a "Próximos pasos" boilerplate at the end of every command skill. The injection varies depending on the integration (`claude` vs `generic`) to optimize context loading without duplicating instructions across all 12 command source files. It also adds phase transitions (updating focus) to specific command files.

## Technical Context

**Language/Version**: Python 3.11+

**Primary Dependencies**: `pydantic` v2, `typer`, `rdflib`

**Storage**: Plain text / Markdown / YAML

**Testing**: `pytest`

**Target Platform**: CLI / Markdown generation

**Project Type**: CLI Toolkit / Agent Integration

**Performance Goals**: Materialization remains instant and idempotent.

**Constraints**: Generated `SKILL.md` files MUST remain within the `agentskills.io` token constraints. Modifying the materializer MUST NOT break the bilingual triggers or fail validation rules.

**Scale/Scope**: Updating 1 materialization module and 12 static markdown templates.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Plain Text as Source of Truth**: Yes, relies on modifying Markdown templates and Python materializer logic.
- **Modern Python Stack**: Yes.
- **src-layout**: Yes, modifications localized to `src/bookwright/integrations` and `src/bookwright/resources/commands`.
- **Plugin-Based Integrations**: Yes, `claude` and `generic` are respected via `Integration.supports_dynamic_context`.
- **Agent Skills Only**: Yes.
- **agentskills.io Standard Compliance**: Yes, the new boilerplate injection maintains standard compliance and avoids token bloat.
- **JSON-over-stdout CLI Contract**: Yes.

## Project Structure

### Documentation (this feature)

```text
specs/022-skills-status-integration/
├── plan.md              # This file
├── research.md          # Research on boilerplate injection
├── data-model.md        # Empty / no new entities
├── quickstart.md        # Materializer validation scenarios
└── tasks.md             # (To be generated by /speckit-tasks)
```

### Source Code (repository root)

```text
src/
└── bookwright/
    ├── integrations/
    │   └── materialize.py       # Core modification: _transform_body
    └── resources/
        └── commands/
            ├── bookwright-bible.md    # Potential focus set injection
            ├── bookwright-outline.md  # Potential focus set injection
            └── ...                    # Other 10 commands (if required)

tests/
└── integrations/
    └── test_materialize.py      # Assert presence of "Próximos pasos" and status checks
```

**Structure Decision**: The modifications fall exclusively within the existing `integrations` namespace and the `resources/commands` static assets. No new modules or structural changes are necessary.
