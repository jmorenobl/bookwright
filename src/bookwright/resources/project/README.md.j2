# {{ title }}

Bookwright project — slug `{{ project_slug }}`.

Author: {{ author }}.

Edit `bible/constitution.md` to declare the project's narrative
contract, then drop your scenes under `manuscript/`. Run `bookwright
check` from the project root to verify the toolchain.
